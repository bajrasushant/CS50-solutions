#include <cs50.h>
#include<string.h>
#include <stdio.h>

int main(void)
{
    long int card_num = 345678923;
    int temp = 0;
    for(int i = 0; i<=strlen(str)card_num; i+2)
    {
        temp = temp + i;
    }
    printf("%d", temp);
    return 0;
}